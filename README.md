# Netflix Clone UI

A responsive Netflix-style landing page clone built with plain HTML, CSS, and JavaScript.

## Run locally

Open `index.html` in your browser.

## Deploy

This project is configured for Vercel static deployment with `vercel.json`.
